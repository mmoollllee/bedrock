<?php

// add_action( 'template_redirect', function () {
//    global $post;

//    if ( !is_user_logged_in() && ($post->post_type != 'page' || !get_field('has_single')) ):

//       $parent = $post->post_parent;
//       $link = $parent ? get_permalink($parent) : get_home_url();
//       wp_redirect( $link );
//       exit;

//    endif;
// });
